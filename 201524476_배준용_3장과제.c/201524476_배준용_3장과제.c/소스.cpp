#include <stdio.h>
#include <stdlib.h>


int main(void) {
	float salary;
	int imate = 2;
	long shot = 53456;
	char grade = 'A';
	float log = 2.71828;	
	printf("The odds against the %d were %d to 1.\n", imate, shot);
	printf("A score of %f is not an %c grade.\n", log, grade);  

	system("pause");

	return 0;
}